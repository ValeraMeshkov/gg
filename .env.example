# Production (GitHub Pages + Render). См. DEPLOY.md
# VITE_API_BASE_URL=https://gg-game-server.onrender.com
# VITE_BASE_PATH=/gg/

# В dev оставьте пустым — запросы идут на /api через прокси Vite.

# Google OAuth настраивается на сервере (server/.env.example), не в Vite.
